# Development ports (this repo). Production uses 5434 / 4000 / 3000.
#
# Development placeholders below are for local setup only.
# Production requires real secrets and rejects placeholders at API startup
# (see apps/api validate-environment / US-011).

# PostgreSQL (docker-compose binds 127.0.0.1:5435 in this repo)
DATABASE_URL=postgresql://mecatrack:mecatrack@localhost:5435/mecatrack_dev

# JWT
JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# API
PORT=4010
NODE_ENV=development
CORS_ORIGIN=http://localhost:3010

# ENFORCE_SECURE_CONFIG=false

# Destructive DB scripts (DEV/test only). Never set to true in production.
# ALLOW_DESTRUCTIVE_DB_OPS=true

# Optional one-time first-admin bootstrap (empty User table only).
# ENABLE_ADMIN_BOOTSTRAP=false
# BOOTSTRAP_ADMIN_EMAIL=
# BOOTSTRAP_ADMIN_PASSWORD=
# BOOTSTRAP_ADMIN_NAME=
